const crypto = require('crypto');\n\nfunction arctan(x, digits) {\n    const precision = BigInt(digits) + 10n;\n    const multiplier = 10n ** precision;\n    const xBig = BigInt(x);\n    let result = 0n;\n    let xSquared = xBig * xBig;\n    let term = multiplier / xBig;\n    let k = 1n;\n    while (term !== 0n) {\n        const currentTerm = term / k;\n        if (k % 4n === 1n) { result += currentTerm; } else { result -= currentTerm; }\n        term = term / xSquared;\n        k += 2n;\n    }\n    return result;\n}\n\nfunction calculatePi(digits) {\n    const pi = 4n * (4n * arctan(5, digits) - arctan(239, digits));\n    const piString = pi.toString();\n    return piString.slice(0, digits + 1);\n}\n\nif (require.main === module) {\n    const digits = process.argv[2] ? parseInt(process.argv[2]) : 100;\n    const piValue = calculatePi(digits);\n    console.log(piValue);\n    console.log(crypto.createHash('sha256').update(piValue).digest('hex'));\n}\n\nmodule.exports = { calculatePi };
